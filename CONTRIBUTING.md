# Contributing

Please keep datasets synthetic or explicitly redistributable. Never commit private conversations, customer records, or credentials.

```bash
npm install
npm run check
npm test
```

Good first contributions include new temporal fixtures, metric tests, visualization notebooks that consume JSON output, and adapters for memory systems.
